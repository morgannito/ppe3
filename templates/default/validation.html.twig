{% extends 'base.html.twig' %}
{% block body %}

<div class="container">
		<div class="row">
                <?php include '../layout/col-left.php'; ?>
				<div class="span9">
                    <h2>Valider mon parnier</h2>
                    <form>
                    <table class="table table-striped table-hover">
                        <thead>

                            <tr>
                                <th>Références</th>
                                <th>Quantité</th>
                                <th>Prix unitaire</th>
                            </tr>
                        </thead>
                        <tbody>
                          {% for leproduit in produit %}
                            <tr>
                                <td>{{ leproduit.id}}</td>
                                <td>
                                    1
                                </td>
                                <td>{{ leproduit.prix}}</td>
                            </tr>
                     
                        </tbody>
                    </table>
                </form>
    
                <dl class="dl-horizontal pull-right">
                  
                    
                    <dt>TVA :</dt>
                    <dd>200€</dd>
    
                    <dt>Total:</dt>
                    <dd>{{ leproduit.prix+200}}</dd>
                          {% endfor %}
                </dl>
                <div class="clearfix"></div>
                <a href="{{ path('commande')}}" class="btn btn-success pull-right">Payer</a>
            </div>
		
		</div>
	</div>

        {% endblock  %}
