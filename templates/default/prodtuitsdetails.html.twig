			{% extends 'base.html.twig' %}
{% block body %}

			<div class="span9">
				<div class="row">
					<div class="span5">
						<img src="{{ prodtuitsdetails.path }}" alt="DevAndClick" width="470" height="310">
					</div>

					<div class="span4">
						<h4>{{prodtuitsdetails.nom}}</h4>
						<h5>AB29837{{prodtuitsdetails.id}} Model</h5>
						<p>{{prodtuitsdetails.description}}</p>
						<h4>{{prodtuitsdetails.prix}}€</h4>
						        {% if app.user is not null %}

						<form action="{{ path('ajouter',{"id":prodtuitsdetails.id})}}" method="get">
					
							<select name="qte" class="span1">
							
							{% for i in 1..100 %}
								<option value="{{i}}">{{i}}</option>

								 {% endfor %}
							</select>

							<div>
							    <button class="btn btn-primary">Ajouter au panier</button>
							</div>
						</form>
						        {% endif %}

						
					</div>
				</div>


	
    {% endblock %}